# 后续 AI 维护约定 · 1.7

用户指定的唯一开发与运行目录是 `E:\A股竞价`。后续源码、配置、测试、文档和数据修改均在本目录进行。原 Codex 任务下的 `outputs/auction-lab` 仅保留为迁移前备份，不再作为开发或运行目录。执行命令时显式将工作目录设为本目录；用户级凭据仍按既有设计保存在工程外。

## 同花顺接入维护（1.7）

先读 `docs/FINANCE_CONNECTION.md`。主系统版本1.7，独立AI助手仍1.2。源码应能在任意可写目录运行；上面的E盘约定仅限本任务维护，不能变成开源安装的依赖。

- 常驻网页 `#finance` 使用 `/api/finance/config` 只保存、`/api/finance/test` 显式单次日历验证。保存、验证、启动监测语义分离；旧 `/api/credentials` 保留兼容的保存并启动。GET状态不联网，不把configured当认证成功。
- 用户文件优先于进程变量和Windows用户变量，更新Key必须重置验证和接入缓存。不读回Key/尾号/摘要给网页，不写浏览器存储；测试用临时凭据，禁止改真实凭据。测试官方日历用max_retries=0，HTTP200/code0且非空合法日期才成功。
- 保存需停止监测、任务完成、live且避开09:10–09:26；测试同样避开演示与保护时段。`/api/demo/exit`仅退出合成演示并恢复本机live视图，不联网、不启动，不让无Key用户困在演示中。
- `run.py --no-auto-start`供查看页面和验收，不得隐式采集。启动器检查旧后台版本并解释重启方法，不自动杀进程。分发前验证新用户无Key界面、已配置未验证与失败状态，并重新打包；不得把本机成功配置当作开源包内置授权。

## 指定板块研究维护（1.6）

先读`docs/SECTOR_RESEARCH.md`。指定板块研究与原报告、竞价/历史回测是不同证据出口，不能因为新增取数而改写原报告或原策略。

- `sector_research.py`只做有界构建，不读密钥、不落盘、不调用模型。官方行业/概念全目录每类一次无分页，服务缓存15分钟；名称只匹配官方目录、代码须精确核验，不将未匹配词自造为官方板块。PCB/MLCC本次已核验为885959.TI/886112.TI，仍以实际官方目录为准，不硬编码名称映射代替核验。
- 选择1—3个唯一完整官方代码。最新同行合并最多300代码、每批100；历史同行按代码顺序每板最多20、合并最多60个短日线请求。构建最多10次最近日或67次历史日业务调用，目录未命中另2次，provider重试另计；不得改为无界成员逐股历史扫描。
- 每板指数只读截至目标日60自然日日线。当前成员`current_members_view`及本机`members_as_of`必须保留，不冒充历史组成或成员生效日。目标日需新官方日历确认，已收盘且当日>=15:10；当前快照不替代旧日，休市推断必须保留provisional。历史股票涨幅用官方相邻交易日前复权收盘，缺日不跳过。
- 同行统计基于有效报价样本；涨停交集基于全部有效当前成员与目标日完整收盘池，两个分母不可混用。成员/池异常保持未知；严格连板另保留consecutive_known_count，全部未知时consecutive_count=null。成交额不是净资金流，尚未公开的capital-flow不可调用。
- JSON每板最多100同行/30涨停，LLM30/30，UI20/20；明示shown_count/truncated并保留原统计分母。未显示不代表未涨停。模型用独立分层白名单`targeted_sectors`，不是把整个raw或未知字段放进通用MARKET_FIELDS。
- `sector_library.py`按date/live/review_id保存独立证据；24位SHA-256前缀编号校验内容，固定路径且读取上限8MiB。旧证据保留，latest指针按日期和报告版本隔离；不原地改基报告，原review_id不变。SSE仅给轻量摘要，详细成员经GET读取。
- 联网搜索/取数只在live及09:10–09:26之外。每个业务请求前后检查generation、模式、关闭和保护；网络不持有行情全局锁，竞价每批流程不调用板块构建。缓存浏览不发金融请求。
- `run_llm(fetch_sectors=True)`须显式sector_codes和review_date/review_id，先检查所选模型Key，再取数归档；全无有效证据返回unavailable并不调用模型。模型发起前再次核验报告版本，部分有效须送缺失和覆盖，不重试付费请求或换供应商。
- AI归档白名单额外保存question(2000字符)、sector_codes、sector_evidence_id、sector_generated_at；Markdown展示并转义，仍校验scope='review'/日期/mode/provider/review_id。输入变化不冒充旧回答已重新分析。独立助手不能因聊天或加载本机摘要隐式取金融数据。
- 回归覆盖sector_research/sector_ai及service/server/library联动；使用临时目录和模拟接口，不调用付费模型、不改真实数据。线上金融接口连通、模型生成、离线通过分别记录，不能混称已全部验证。

## 历史研究与每日核验维护（1.5）

先读 `docs/BACKTEST.md`。真实竞价批次、实际时点排名、按旧权重重放、按当前权重实验、涨停池延续基准必须明确区分。未取得真实历史竞价，不得宣称已有优化胜率。

- 盘前 `research_manifests` 与时点 `research_decisions` 使用 INSERT OR IGNORE；后来的上下文、配置或结果不能覆盖。当日首次准备晚于09:15不具备严格盘前资格。清单记录完整昨日池、官方前一交易日、采集代码及当时权重。
- 每批继续即时保存和评分，批次附 `_strategy_weights`。09:24:50与09:26的轻量排名快照只在引擎尚未消费截止之后的批次时冻结；接收时刻正好等于截止的批次应先纳入。不能让重放、调参、模型或大文件导出阻塞竞价。
- 09:27后整理每日不可变档案；有当时排名用 `recorded_ranking`，无则按批次记录权重重放并标 `replayed`。原始批次永不替换为回放输出。保存引擎源码、数据和清单摘要；缺旧权重不能冒称当时策略。
- 15:10后生成的完整同日涨停池仅作标签，缺池/重复代码/盘中池均未知。结果版本单独新增，不改原分。15:00–15:09手动复盘不能触发收盘标签；自动复盘仍需15:10后重新获取。
- 回测候选固定上一交易日全涨停池；默认09:24:50，09:26仅收尾核验。使用实际接收顺序和时间，未来批次不纳入；拒绝/倒序记录使严格资格降级。演示绝不进入真实优化。
- 当前七因子回放与原时点档案独立。权重实验固定七因子齐全的共同样本，按交易日训练/验证/保留检验，不随机拆股票；门槛30完整日/300股日/开发集正负各30，不得为获得结果放宽。
- 最多64组预设权重、三折扩展日期验证、末20%且至少5日留出。保留集不能选参数；跨实验已用检验日期台账使结果降为探索。相同数据/代码/权重复用同一档案，不重复声称新验证。因子诊断也仅使用开发集。
- 外部导入隔离在research目录，不写live批次表，不覆盖旧日期；校验完整代码、单位、带时区时间及显式来源声明。外部真实性仍不可独立证明。空模板不可包含虚构行情。
- 对其他AI优先提供 `/api/research/ai-dataset` 开发日期白名单；全日原始序列/每日收盘结果只作审计，不能把保留集标签送入调参。`/api/research/proposals`仅归档七权重建议，永不调用LLM或自动启用；需新日期验证。
- 研究和日档在data内，禁止随Git或源码包分发。回归覆盖research/replay/optimization/daily_validation与service/server，使用临时数据；保留当前真实无历史的不足状态。

先读 `README.md`、`docs/STRATEGY.md`、`docs/DATA_SOURCES.md`、`docs/ARCHITECTURE.md`，修改官方补充观察时另读 `docs/OFFICIAL_UPGRADE.md`。本目录是完整独立应用，`work/` 的上游仓库和官方全文缓存只是开发资料，不是运行依赖。

- 真实行情只来自用户授权的数据服务，不把演示数据切入实盘，不拿静态示例替代接口失败。
- 不把任何 API Key 写进项目、测试、提示词、日志或最终文档。只读用户环境或用户级凭据来源；不扫描其他任务寻找密钥。
- 官方成功必须同时满足 HTTP 200 与 `code==0`。完整读取分页；失败/null 与空池区分。
- 竞价每批到达立即计算并发布，禁止为方便改成收十分钟后统一分析，禁止把 LLM 放入采集关键路径。
- 竞价 `data.timestamp` 是响应组装时间，不能标为交易所行情时间。普通价格快照另遵循官方日期契约与明确的有限休市日推断；不要将所有接口timestamp都称为组装时间。
- 缺值保持空，报告覆盖；`5天4板` 不当作 4 连板；官方天梯每层 4 只不能作全市场晋级分母。
- 板块成交额不是资金净流入。当前官方资金流接口不可公开调用；新增来源先核对授权和口径。
- 历史报告必须核对每种数据日期；当前成分表不伪装历史成分，当前行情不填历史日期。
- 默认重点池为自选、昨日完整涨停和趋势强股；按来源去重。增删自选通过专门端点，不reset同日观察；移除manual来源后自动来源可能仍在。新关注只影响下一轮，不能补采此前未观察的竞价。
- 输入代码是字符串，保留前导零。六位或完整代码必须经官方A股搜索精确确认；不猜后缀、不使用名称模糊匹配。只有完整代码的缓存不能证明裸代码唯一。
- 独立查询不等于加入关注。`stocks.analysis.date` 是日线截止日，`stocks.analysis.auction.date` 是本机竞价会话日，必须分别展示；历史查询不另外拉取当前快照来填历史数据。
- 竞价七因子、stocks日线四因子和selection四因子筛选分是三套公式。保持独立标签、权重、阈值与版本；selection硬条件不是日线查询的准入条件。覆盖率按各模块定义，不能将权重覆盖改成因子个数占比。
- 个股查询为一次日线请求/最多40交易日。趋势筛选最多60候选、30入选，每股60自然日前复权窗口；保留有限样本及前复权可修订说明，禁止改成无界全市场历史循环。
- 09:10–09:26（含整分钟）优先竞价：个股历史延后09:27，趋势筛选在新请求前检查保护/取消并保留partial。中断后应可自动重试，不能用中断结果覆盖同日已完成缓存。SSE和已有本机观察继续可用。
- 收盘复盘完成后刷新趋势池，可引入成交额活跃的非涨停股；下一会话的新池不能反向替换当前会话原来的昨日趋势来源。只合入与会话上一交易日匹配的池。
- 缓存使用 `stock-metadata.json`、`trend-pool.json`、`stocks/完整代码-日期.json`；selection纯计算模块不写文件，由service负责原子持久化。仅因日期推断或短历史而partial、但候选都处理完成的缓存可复用；中断、历史读取错误和池缺失不算完成，不能每次重启无谓下载同一批60股日线。
- 演示拒绝真实查询、自选变更和趋势刷新，不展示真实个股信号为演示结果。
- 修改策略时文档、配置、页面说明和测试同步。`python -m unittest discover -s tests -v`；浏览器检查空数据、演示、错误和连接状态。
- 原始 SQLite 实盘记录不得清空或覆盖为测试数据；测试使用临时库。mode 隔离。
- 交付时区分离线通过、线上认证验证、真实竞价十分钟运行验证，不能把其中一种说成全部完成。

修改接口时同时维护 `CONTRACT.md` 和架构文档；股票模块相关回归包括 `tests/test_stocks.py`、`tests/test_selection.py` 与service/server测试。重点检查精确解析、缓存歧义、保护时段、取消、分批新增、历史日期隔离和演示隔离。不要修改已有真实验收记录来掩盖未通过的测试。

## AI 接入维护（1.2）

- 先读`docs/AI_ASSISTANT.md`。DeepSeek、OpenAI、自定义配置分别存储，内置地址固定；不将金融Key用于模型。
- 两个本机进程共享`ai_gateway.py`，配置采用跨进程锁/原子替换。测试用临时凭据目录，禁止写假Key覆盖用户配置。
- OpenAI使用Responses并解析message/output_text，忽略reasoning；DeepSeek/自定义使用Chat Completions。模型ID必须查官方文档，不凭记忆沿用已退役名称。
- 发送时固定当前provider/config/history/context，切换只影响后续请求。不能将另一服务商历史自动转发；失败不自动切换或重试付费请求。
- 主系统市场分析与独立助手均只经`llm.build_summary`出口。维持日期/模式/覆盖/缺失标记，新增摘要字段同步白名单测试。
- 独立助手默认不发市场数据，先加载预览再勾选；缓存报告明确无实时竞价。单独运行不能隐式启动行情采集或请求金融API。
- 密钥不进前端状态、浏览器存储、项目、聊天持久化、异常日志或分发包。HTTP主程序头为auction-lab，助手为auction-ai。
- 没有用户对应AI Key时只能报告协议模拟/本机验收通过，不能声称真实DeepSeek/OpenAI生成已验证。

## 本机报告库与诊断维护（1.3）

- 新接口见根目录`CONTRACT.md`，架构见`docs/ARCHITECTURE.md`。`reporting.py`、`insights.py`保持纯计算；历史列表/读取/比较/Markdown保存及就绪检查不增加金融API请求，不修改竞价每批读取、落盘、评分和发布顺序。
- `review`是用户历史视图；`latest_review`供自动复盘和最新趋势准备使用。读取旧报告不改latest_review，自动完成不能抢走历史视图，自动任务不得因视图日期旧而重跑当日取数。
- **报告对象发布后不可原地修改。** `_review_identity()`按对象身份缓存指纹；修订必须新建完整对象再替换。`review_id`包含公开证据及生成/完成时间，排除raw和导出附加信息；同日重新生成不能沿用旧AI附录。
- 保存和专属AI请求携带日期、provider及review_id，校验过期版本。AI附录必须`scope='review'`且日期、模式、review_id、provider全匹配；market综合盘面回答不可附到某日报告。报告专属AI摘要不能掺入当前竞价/个股。结果档案只写白名单，不得写模型配置、Key或请求头。
- 报告与AI按live/demo隔离。报告JSON旧格式缺mode只在已核验存储模式下补齐；比较函数本身拒绝缺模式。历史读取禁止未来日期及生成中的切换，不以当前数据补历史证据。
- Markdown文件名只用严格有效日期和固定provider枚举，下载不接收任意路径；保持原子替换、大小上限及sha256校验。前端使用保存响应的download_url，不能绕过校验下载后来的同名版本。sha256是内容一致性标识，不是授权令牌。
- Markdown保留日期依据、覆盖、严格连板下限、全部公开质量提示；行情文本和AI正文需转义。正文前30或观察清单前10不能冒充全市场名单；不自动把观察清单加入自选。附录或对比不能改变报告证据指纹。
- 两期比较先核验完整池数量、代码及模式；缺失不当空池、零不当缺失。使用“共同两期涨停/当期新出现/从对比池退出”，不自行改称晋级/首板/断板。相邻性只从官方日历证据确认，严格连板只用consecutive_days并保留下限。
- 板块先匹配全体共同代码再截取展示30项；日期不对应不计算变化，日期推断标provisional。零成交额基准不能计算增长百分比，样本排名与成交额变化不是净资金流。
- 就绪检查只看snapshot与内部准备日期，不为了“检测”读取凭据或发远程请求。未知mode报错，今日日历未核验则提示未知，不按星期猜开市；休市不要求终态；仅自选模式不要求趋势池。检查通过不能宣称真实十分钟采集验收通过。
- SSE有变更立即推送；闲时保持轻量keepalive和15秒完整刷新，09:10–09:26保护时段3秒刷新。不要将降频用于延后已到批次，也不要把页面刷新间隔描述为上游刷新承诺。
- 报告相关回归使用临时数据库与临时目录，覆盖历史视图与自动任务分离、模式隔离、报告版本失配、AI附录绑定、同名文件被替换后的校验、路径越界/超限、部分数据及空池。不要为验收写入或覆盖真实行情记录、凭据或原有验收记录。

## 官方指南升级维护（1.4）

- 官方参数与单位优先核对 `https://fuyao.aicubes.cn/llms-full.txt` 及具体 REST 字段表；最佳实践只作产品参考。示例12的“无炸板池”已过时，不能因此删除已开放的炸板池；`continue_day_cnt`与严格连续板数仍须区分。
- `sentiment.build_sentiment(report)`保持纯计算，不取数、不落盘、不读时钟。旧报告只通过公开`review_sentiment`按报告对象缓存，不原地追加字段、不改变原`review_id`。新生成或主动补充的报告才持久化`sentiment`。
- 情绪矩阵只使用已留存完整涨停池和日历，最多10日；官方天梯每板最多4只不能替代全池。缺失日不补零，区间边界为下限；1板下限不能宣称已确认首板。
- 封单分布基于`seal_money/max_seal_money`，与竞价金额留存分开；缺值、峰值≤0、负值、当前额大于峰值均不参与。涨停原因按`limit_up_reason`完整原文精确分组，不拆成行业或推断因果。
- `official_context.build_official_context`仅允许风向标及三类龙虎榜共4个业务分项，每次都传显式日期、请求前检查取消。provider常规重试可能增加HTTP次数；禁止改成自动扫描一年、成员表归因或竞价轮询内调用。
- `POST /api/reports/enrich`须校验live、已收盘、报告原始日历、日期、`review_id`和保护时段；任务键`evidence`。取消、全部失败、版本冲突保留原报告；已有ready补充不被partial覆盖。补充不能将原复盘`status`升级为ready。
- 风向标`date`与`date_ms`须精确对应上海零点；`auction_pct`是百分数原值。样本均值、中位数和正涨比例不是全市场指标，标签不进入原竞价分。timestamp为组装时间，不用它证明交易日。
- 龙虎榜核对`trade_date`和`board_type`，timestamp按目标日零点另作交叉检查；不一致或缺失保留原值并partial，不静默修改。官网示例时间戳与日期自身存在不一致，不能将示例数值硬编码为规则。
- 龙虎榜`range_days=1/3/其他`严格分开，三榜不能相加；同代码同区间的多记录保留、标记、不猜去重累计。游资从`hot_money_items[].rows`展开，`buying`是游资聚合净额，不按每行累加；金额元、比例小数与派生_pct分开。
- 新补充结果用报告深拷贝替换并持久化，改变`review_id`且旧AI附录失效。网页递归去raw；Markdown保留状态与口径；LLM只走显式白名单，每榜每区间最多10条，不能把raw或未知上游字段送模型。
- 429/4001冷却在同进程同Key的provider间共享；竞价单次请求不在网络层重试。Retry-After是工程兼容处理，不宣称官网保证有该响应头；不能因“不限累计次数”移除节流或承诺刷新频率。
- 本轮回归除已有套件外覆盖`test_sentiment`、`test_official_context`及新增service/server、Markdown和AI白名单测试。测试使用临时数据；不修改既有真实记录或把协议模拟写成实盘验收。
