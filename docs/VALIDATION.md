# 验收记录

## 1.5 历史回放与每日闭环（2026-09-20）

- `python -m unittest discover -s tests -q`：435项通过；`node --check static/app.js`通过。新增回放/优化/日档/研究与HTTP集成测试均使用明确的合成夹具和临时目录，未写入真实实盘表。
- 覆盖固定昨日候选、实际接收时点、等于截止时刻的批次、未来观察排除、缺标签未知、演示隔离、共同样本、按日期三折验证、保留期标签不影响选参、跨实验重复检验台账、原评分与重放区分、不可变日档、原权重来源、导入幂等及拒绝覆盖、AI开发集白名单和建议不自动应用。
- 本机真实数据核查：只有2026-01-05的演示竞价批次，live竞价日期为0；留存2026-09-07至09-18十个完整交易日涨停池。可计算9组相邻延续：509个昨日涨停股票日，100次继续进入次日涨停池，加权延续率19.6464%。这不是竞价算法命中率，不能据此调整权重。
- 实际运行本地研究后正确显示`insufficient_data`，候选权重为空、实盘配置未变。浏览器检查通过：策略回测空态、每日存档空态、运行按钮、真实池延续明细、来源说明与保存入口。实际Markdown下载HTTP 200且文件已在本机实验目录；AI出口`scope=development_only`且当前sessions为空。
- 真实系统自动采集为开启、`auto_review=true`、时间15:10；09:27冻结与15:10核验的时间控制已有离线集成覆盖。**本轮没有真实交易日09:15–09:25全程采集或真实盘后自动闭环验收，也没有足量真实竞价可做有效参数优化。** 不把离线测试或页面验收等同于实盘效果。
- 原始SQLite、研究档案、参数建议和凭据不进入Git或源码分发包；旧版验收记录保持原样。

**当前部署状态（2026-09-19迁移后）：** 完整系统已复制到 `E:\A股竞价`，旧实例已退出，新实例从该目录运行。文件与数据库迁移校验通过。此前授权的Key现已保存在本机用户凭据文件，官方连接验证成功；下文初次交付的权限限制是历史记录，当前状态以 [迁移记录](MIGRATION.md) 为准。

验证日期：2026-09-19，Asia/Shanghai。当天为周六，官方日历最近交易日为 2026-09-18。

## 已完成

1. **157 项自动测试通过（1.1）**：竞价逐批计算、09:20/09:25边界、终态状态兼容、空值与旧数据、迟到/重复响应、跨日、分页、限流与重试、真实/演示隔离、完整涨停池晋级率、日线缺失与未来数据、休市日期推断约束、凭据不进入模型摘要、本机跨站请求保护等；新增代码消歧、热添加关注、整批失败不写入、趋势有限候选筛选、保护时段延后、模型跨模式返回隔离、完成后的部分质量缓存恢复等。
2. 前端 JavaScript 语法通过。浏览器已验证 SSE、演示逐批更新至 09:25、个股因子和观察曲线、参数保存、空态以及真实复盘页面。
3. 使用用户明确授权的 Key 成功调用官方标的搜索、交易日历、竞价 live/final、涨停池、跌停池、炸板池、连板天梯、股票/指数行情、板块目录、成分与股票历史日线。
4. 实际生成 **2026-09-18 收盘复盘**，原始响应保留在 JSON。

| 实测项目 | 结果 |
|---|---|
| 交易日历 | 242 个交易日，最新 2026-09-18 |
| 当日涨停池 | 77 只 |
| 当日炸板池 | 25 只 |
| 严格连板 | 12 只，最高 4 板 |
| 昨日涨停延续 | 47 只中 12 只再次涨停 |
| 全市场行情 | 5,575 条，56 页 |
| 行业与概念目录 | 710 个，全部获得排行输入 |
| 板块成分归属 | 强度前 30 个板块 |
| 重点股日线 | 请求 20 只，20 只有目标日数据；18 只全部周期指标齐全 |

以上计数用于验证系统读写与计算，不构成推荐名单。全市场与板块快照日期存在下述推断限制。

## 实测与文档的差异

- 周末竞价 live/final 均返回 `auction_phase=closed, data_status=final`；官方文档示例为 `final/ready`。系统兼容两者，09:25 前不会认可终态；周末不将该响应计为当日实时竞价。
- 全市场及板块快照在休市日返回 2026-09-19 时间，而文档描述为上游最新有效时间。系统保留原始时间，仅在严格日历条件下推断最近收盘日为 2026-09-18，并设置 `date_verified=false`、`data_status=provisional`、报告 `partial`。这不是已经独立验证的交易日归属。页面和导出文件均有提示。
- 当前官方主力资金流接口明确不可公开调用，未请求该受限接口。板块榜为量价代理指标，净流入显示不可用。

## 尚未完成的验收

- **真实交易日 09:15–09:25 完整十分钟采集尚未实测**。已有测试只能证明本机逐批计算、流程和时间边界；不能证明实盘上游刷新频率、账号全天额度和终态到达时延。
- 外部 LLM 接口完成协议与安全边界测试，用户尚未配置独立模型服务，未实际调用付费模型。
- 未进行交易策略收益回测，评分不代表上涨概率。

## 凭据保存状态

本次 Key 已获用户授权并完成线上验证，只存在于运行服务的进程环境。当前工具环境未授予 `%APPDATA%\hithink-finance` 凭据文件写权限，**没有声称已永久保存**，项目/文档/压缩包不包含 Key。

用户自行从 Windows 双击 `启动系统.cmd` 后，可在「策略与接入」通过密码输入框保存用户级凭据；保存后后续启动自动读取。若复用本次仍在运行的服务，界面会显示“本次会话已连接，尚未保存”。

## 下一交易日操作

09:10 前启动并确认重点池准备完成；保持联网和不休眠。09:15 检查观察次数随批次增长；09:20 检查后段因子；09:25 查看已有排名及终态覆盖；09:26 检查未完成项是否明确保留。15:10 后查看复盘日期与来源。全过程保存到本机数据库。

## 1.1 追加功能实测

2026-09-19 使用同一已授权接口完成以下验证，未提交交易或调用外部 LLM：

- `600519` 经官方 A 股检索解析为 `600519.SH`，生成截至 2026-09-18 的独立日线分析，40 根日线、四因子覆盖100%。`000001` 解析为 `000001.SZ`，未混入同代码指数。
- 浏览器点击「加入关注」后，自选数由0变1，采集范围由90变91；点击「移除自选」后恢复90，原自选列表仍为空。测试用股票没有留作用户自选。
- 趋势池候选60、已分析60、完整核心日线指标57、符合筛选19；与昨日完整涨停77只合并去重后重点池90只。候选同时包含报告活跃股和最近5日涨停股；复用缓存中的完整涨停池，没有把天梯截断数据当全池。
- 趋势状态为partial：活跃股来源包含休市日期的明确推断，3只日线不足或缺失。页面展示原因，不把不完整数据填成0。
- 单股页面实际检查了代码解析结果、日线图、均线和因子解释、无竞价记录空态、来源标签及新增/移除反馈；JavaScript语法通过。周末跟踪状态表示等待交易日窗口，不声称正在采集周末竞价。
- 保护时段逐批采集、历史分析延后、跨日趋势名单和模式隔离通过离线测试；**完整真实早盘十分钟仍须在交易日验收**。

新增缓存和单股JSON保存在应用data目录，文件与分发压缩包不包含API Key。源码与文档版本为1.1。

## 1.2 独立 AI 助手验收（2026-09-19）

- 全项目 **200 项自动测试通过**，包括两家协议适配、凭据隔离、跨进程并发保存、拒绝重定向、错误脱敏、部分回答标记、白名单市场摘要、独立会话及跨窗口provider固定。
- 两个JavaScript文件语法通过。浏览器实际验证：DeepSeek与ChatGPT切换后模型正确变化、未配置Key时给出明确提示并禁止生成、独立助手加载2026-09-18本机盘面摘要且默认不发送、主系统共享同一组选项。
- 主系统保持90只重点池、原2026-09-18复盘、live模式，新增AI模块未改变股票策略。AI配置损坏也不会使主行情状态/SSE不可用。
- 独立助手监听127.0.0.1:8766，健康标识auction-ai-assistant；主系统仍在8765。重新启动复用正确健康标识，两个应用均从E盘运行。
- 新分发包仅包含源码、界面与说明，不包含Key、聊天记录、行情数据库或用户配置。
- 用户尚未提供DeepSeek/OpenAI的API Key，**未验证这两个账户的真实授权、余额或付费回答**；模拟协议测试和本机界面通过不能等同线上生成成功。

凭据状态更新：上述早期“未永久保存”的记录已在目录迁移时解决，详见MIGRATION.md。同花顺Key现已保存至用户级凭据目录；此次新增的DeepSeek和OpenAI配置仍待用户在界面填写。

## 1.3 收盘报告与日常检查验收（2026-09-20）

- 全项目 **249 项自动测试通过**。新增覆盖 Markdown 日期/模式/缺失与零值区分、文本转义、完整质量说明、同报告版本 AI 附录、历史本地读取、保存文件原子替换及下载校验、两日完整池比较、板块日期与样本边界、盘前就绪状态，以及 SSE 闲时保活。
- 主界面与独立助手 JavaScript 语法通过。额外前端行为检查覆盖显示日期与输入日期分离、过期 AI 不可附入、下载端点同源校验、任务完成后历史列表刷新，以及报告正文避免重复重绘。
- 实际浏览器点击「保存 Markdown」，成功写入 `E:\A股竞价\data\reports\2026-09-18.md`；UTF-8 中文正常。下载接口返回 `text/markdown` 附件，响应字节与已保存文件及 SHA256 一致。页面显示本地保存路径和「再次下载 Markdown」。未将浏览器下载目录落盘作为已验证事项，下载行为由浏览器设置决定。
- 实际页面读取 2026-09-18 本机历史报告成功；当前仅有一个已保存日期，两日对比正确显示「暂无更早日期报告」并禁用对比。两期完整、缺失、不相邻、日期推断及模式不同的比较由隔离测试验证，没有伪造第二份实盘历史。
- 实际展开「盘前就绪检查」，正确显示凭据已配置、今日交易日历判定休市、自动监测已启动；不会把休市日无竞价终态判成采集故障。该检查不进行远程鉴权或行情请求。
- 实际浏览器检查后未发现控制台警告或错误，实时推送已连接；主系统保持 live 模式、90 只重点股和原 2026-09-18 复盘。主程序健康版本为 1.3.0，独立 AI 助手继续使用 1.2.0，无需为报告功能重新配置密钥。
- Markdown 中相同来源提示先合并摘要，完整对应代码/字段位置移至文末；真实报告正文未被数百条重复板块日期说明淹没，缺失项和原始 JSON 仍保留。

联网核对官方能力和浏览器行为的来源见 [报告使用说明](REPORTS.md) 与 [数据口径](DATA_SOURCES.md)。本轮保存、历史读取、比较与本机检查没有新增收费模型调用；**真实交易日完整十分钟竞价、两家 AI 的实际付费生成及策略收益回测仍未完成验收**，沿用前文边界。

## 1.4 官方指南升级验收（2026-09-20）

本轮依据官网接口总览、最佳实践及用户补充的 `https://fuyao.aicubes.cn/llms-full.txt`，新增情绪结构和按日官方观察。以下真实接口结果使用既有已授权凭据取得，没有将测试夹具写入实盘数据。

- 真实启动主程序1.4.0与独立助手1.2.0，均从`E:\A股竞价`运行。官方日历、重点池准备成功；实盘模式、90只重点股、19只趋势入选保持正常。
- 为既有2026-09-18报告补充四个分项，均成功返回目标日数据：风向标6只；全部龙虎榜55条/50股；机构榜33条/30股；游资上游52条/47股，按14个游资展开42条/30股。最后两组计数范围不同，不能相除推定覆盖率。龙虎榜trade_date与上海零点timestamp一致；没有跨榜或跨1日/3日区间求净额合计。
- 原报告行情仍保留partial及休市快照日期推断提示，新增观察没有把原盘面升级为完整数据。原涨停77只、严格连板12只及原始行情证据保留。情绪结构读取10个已保存完整池；封单留存有效77/77，超过100%异常0；官方原因77份原文按文本分组，只展示12组。
- 浏览器验证新增统计和官方观察的位置、日期标签、分组和金额单位；已取得实时推送，未发现控制台error。龙虎榜默认折叠，按需展开，避免长表遮挡原强弱榜。
- 实际点击原因组中的`603118.SH`，查询日期自动为2026-09-18，返回该日截止的40根日线和独立四因子；没有改变用户自选。历史日线日期与本机竞价会话日分别显示，未用周末数据填历史。
- 实际点击「保存 Markdown」，本机2026-09-18.md包含连板矩阵、封单留存、原因分布、风向标和三类分区龙虎榜；中文与文本转义正常。页面显示保存成功与再次下载链接；最终78332字节Markdown下载响应与本机文件和SHA256一致。浏览器下载目录是否另行保存由浏览器设置决定。
- 模型摘要保留上述日期、口径、缺失与分区，每榜每区间最多10条；真实结构摘要约83K字符（含原复盘榜单），无raw响应。未配置两家模型Key，未调用付费AI。

全项目 **337 项自动测试通过**，主界面和独立助手 JavaScript 语法通过。复用已留存的真实原始响应做新增数量校验：全部/机构榜为verified；游资展开覆盖为unknown，合法范围差异不降级、不补造记录。该次复核未新增行情请求。

本轮离线回归额外覆盖分页回显与总数变化、同Key限流、90秒Retry-After不提前采集、损坏池、连板下限、异常封单、日期/单位契约、空榜和缺失、取消与版本冲突、AI白名单、Markdown保存失败后报告版本一致及本机重试。源码、文档和两个分发包均不包含行情数据库、研究缓存或凭据。

真实交易日09:15–09:25完整十分钟采集仍未实测；周末接口验证、离线测试和页面检查不能替代该项，也不代表策略收益或模型账户授权已验证。

## 1.6 指定板块联网研判验收（2026-09-20）

- 全项目 **485 项自动测试通过**。本轮新增50项覆盖板块目录、报价日期、历史样本限额、完整涨停池交集、全失败不调用模型、报告版本变化、缓存/导出、文件摘要校验、本机HTTP保护、AI白名单及Markdown来源。测试使用临时目录与模拟网络，没有覆盖用户凭据或实盘记录。主页面JavaScript语法通过。
- 使用已授权同花顺凭据真实搜索，分别确认 `PCB概念 / 885959.TI` 与 `MLCC概念 / 886112.TI`。两者确实在官方目录中；旧AI摘要前30板块的缺失不能用于推断板块不存在。
- 浏览器实际搜索、选择两板块并点击「只读取板块数据」，成功保存证据 `7462748e610f6e1cae0d60af`，绑定已有2026-09-18报告版本。PCB当前成员238只、目标日报价238只（有效涨幅237只）、完整涨停池交集3只；MLCC当前成员38只、报价38只、涨停交集1只。两者指数日线有效；成员历史表现仍按当前名单观察，报价继续保留原报告休市日推断标记，整体partial。
- 本次构建5次业务调用（日历1、成分2、指数日线2），报价复用已核验同日报告缓存，另此前搜索加载官方目录2次。未调用收费模型；离线工作流验证先取数再生成和提交时固定服务商配置。真实模型生成仍需用户点击分析，不能把摘要和模拟调用验证等同付费生成成功。
- JSON导出与模型摘要检查成功；模型收到两板块各30条同行以及3/1条涨停成员，完整统计、有效数、当前成分口径和截断标记保留。原复盘版本未改变，已有AI回答未被覆盖。界面已展示卡片、来源与旧回答提示。
- 源码位于 `E:\A股竞价`。旧8765进程的结束并重启操作被自动审批以“被策略阻止”拒绝，未绕过；本轮在独立8767端口运行1.6.0完成真实验证。8765仍为旧进程；两处采集在验收结束时均停止，既有监测停止状态被保留。关闭旧服务或重启电脑后，原一键启动入口可从新源码启动8765。新页面可通过 `http://127.0.0.1:8767/#review` 立即使用。

此次仅验证板块取数及研判接线，不新增策略收益、实时竞价十分钟或付费模型回答的验收声明。真实行情、证据、凭据及AI历史均不随源码分发。

## 1.7 同花顺接入与开源首次使用验收（2026-09-20）

- 全项目 **510 项自动测试通过**，包含22项新金融接入测试与3项启动器检查。覆盖临时凭据保存、用户文件优先、无密钥状态、严格输入、保存不采集、HTTP同源限制、单次官方协议验证、错误脱敏、并发、换Key旧结果失效、保护时段、演示退出、旧入口兼容和no-auto-start。全部自动测试使用临时数据/模拟网络，不写真实Key或报告。
- 实际浏览器在独立 `http://127.0.0.1:8768/#finance` 检查1.7.0网页：常驻同花顺接入、官方申请链接、密码输入、独立保存/测试，以及凭据来源/验证信息均可见。打开时准确显示「已保存未验证」，没有回显已保存Key。
- 13:07实际点击「测试同花顺连接」后，仅发起一次官方交易日历业务请求，使用用户已授权的既有凭据。返回241个有效交易日、最近2026-09-18，耗时3626毫秒，页面由验证中转为验证成功。该结果只证明此次认证与日历读取，不代表所有数据权限或付费模型生成已验证。
- 新进程以 `--no-auto-start` 启动，验证后仍为live、监测停止，保留已有2026-09-18报告；没有启动竞价或全市场取数，没有调用模型或覆盖用户凭据。8765旧服务和8767旧预览未终止，沿用上轮审批边界；当前可直接使用8768新入口。新启动器遇旧后台会提示重启，不再悄悄复用不支持新接口的服务。
- 新电脑可在任意可写路径解压运行，Windows缺Python时启动脚本显示安装说明并暂停。金融与AI凭据均在项目外，源码和分发包不附带个人账户、数据或Key；首次接入与本机端点说明见 `FINANCE_CONNECTION.md`。
- 另用临时目录、隔离凭据和模拟上游跑真实本机HTTP全流程：无Key→演示→退出→保存测试Key→日历成功→认证错误→HTTP503；无自动start、无密钥响应泄漏、无真实金融调用。浏览器8769实际确认空白新用户「待配置」、测试按钮禁用及演示退出后可返回配置；该临时服务自动关闭，不用作用户入口。
- 两个源码包重新生成并检查：主包86个文件、AI独立包14个文件；新指南、接口、启动器和测试已包含，未发现用户金融Key内容。已有2026-09-18报告证据摘要保持 `9bc51039b3997e91c9147ceb26e9deb1d97fedfc09ffcab379891b24ad48b14f` 不变。

真实交易日09:15–09:25完整采集、策略样本外收益及实际付费AI回答仍不在本轮新增验收范围。
